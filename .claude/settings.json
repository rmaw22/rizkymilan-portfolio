{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "file=$(jq -r '.tool_input.file_path // empty'); echo \"$file\" | grep -q 'src/data/' && npm run type-check 2>&1 | tail -5 || true",
            "timeout": 30,
            "statusMessage": "Type-checking data files..."
          },
          {
            "type": "command",
            "command": "file=$(jq -r '.tool_input.file_path // empty'); echo \"$file\" | grep -q 'src/data/' && npx vitest run src/__tests__/data.test.ts 2>&1 | tail -6 || true",
            "timeout": 30,
            "statusMessage": "Validating data integrity..."
          }
        ]
      }
    ]
  }
}
